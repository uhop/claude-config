{
  "permissions": {
    "allow": [
      "Bash(vault-curl:*)"
    ],
    "deny": [
      "Bash(git push:*)",
      "Bash(git tag:*)",
      "Bash(npm publish:*)"
    ],
    "defaultMode": "auto"
  },
  "enabledPlugins": {
    "typescript-lsp@claude-plugins-official": true,
    "clangd-lsp@claude-plugins-official": true
  },
  "effortLevel": "xhigh",
  "tui": "fullscreen",
  "skipAutoPermissionPrompt": true,
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "$HOME/.claude/hooks/git-commit-gate.sh"
          }
        ]
      }
    ],
    "SessionStart": [
      {
        "matcher": "startup|resume",
        "hooks": [
          {
            "type": "command",
            "command": "$HOME/.claude/hooks/vault-resume-hook.sh"
          }
        ]
      }
    ]
  },
  "mcpServers": {
    "vault": {
      "command": "npx",
      "args": [
        "-y",
        "@uhop/vault-storage-mcp@latest"
      ],
      "env": {
        "VAULT_API_URL": "${VAULT_API_URL}",
        "VAULT_API_TOKEN": "${VAULT_API_TOKEN}"
      }
    }
  }
}
